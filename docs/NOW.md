# 当前未完成事项

本文件只保存已接受但尚未完成的工作。事项落地后在同一交付中删除，历史由 Git、PR 和决策记录保留。

## MOB-NOW-001 收窄整服务器物理删除能力

- 目标：删除当前没有生产调用者的 `ServerProfileDao.delete(serverId)`，以及只为证明级联删除而存在的测试路径。
- 依据：`MOB-PAIR-001` 与 `MOB-DEL-001`；普通配对和同步路径不应持有整服务器级联删除能力。
- 当前证据：DAO 暴露 `DELETE FROM server_profiles`；Room 外键会级联影响草稿、outbox、附件传输、通知、停止请求和投影；生产代码没有调用该方法。
- 下一步：使用 `$babel` 建立任务合同，确认所有调用和迁移影响后修改 DAO 与相关测试。
- 停止条件：若发现正式产品入口依赖该能力，或删除会改变用户已接受的“忘记服务器”功能，停止并核对语义。
